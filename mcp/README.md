# MCP Platform SDK

Python SDK and reference MCP servers for the mcp-platform monorepo.

## Install

```bash
pip install -e ".[dev]"
```

## Run tests

```bash
pytest tests -v
```

## Filesystem server

```bash
MCP_FS_ROOT=/tmp/workspace mcp-filesystem-server
```
